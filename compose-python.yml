version: '3.8'

services:
  python-fastapi:
    build:
      context: ./python-fastapi
      dockerfile: Dockerfile
    container_name: benchmark-python
    ports:
      - "3003:3000"
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M